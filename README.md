# Bonsai
In Bonsai, a tree becomes itself through the branches you choose to keep. Every idea gets its own branch, and you decide which ones grow. Bonsai turns AI coding sessions into a visual tree of experiments; each branch its own agent, its own code, its own conversation.

# Bonsai artwork

`bonsai.svg` is the supplied tree artwork, with a cropped viewBox and theme-aware favicon colors. The app uses the same file as a currentColor mask. The original is preserved in `docs/design/bonsai-mark.svg`.
